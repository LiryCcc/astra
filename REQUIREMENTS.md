# Astra Flutter 应用 — 需求文档

## 一、项目概述

跨平台 Material 风格应用，目标平台：**Android、iOS、Windows、macOS**。同一套代码适配手机、平板、桌面，包含三个页面路由，支持主题（日间 / 夜间 / 跟随系统）与三语（中文 / 英文 / 日语）。

---

## 二、平台与设备

| 维度 | 要求 |
|------|------|
| 移动端 | Android、iOS（手机 + 平板） |
| 桌面端 | Windows、macOS |
| 响应式 | 手机、平板、桌面三种布局形态 |
| 最低版本建议 | Android API 21+、iOS 12+、Windows 10+、macOS 10.14+ |

---

## 三、页面与路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | 首页 | 欢迎/概览、快捷入口、简要统计 |
| `/settings` | 设置页 | 主题、语言、关于等 |
| `/todos` | TodoMVC | 经典 Todo 增删改查、过滤、持久化 |

---

## 四、主题模式

| 模式 | 行为 |
|------|------|
| 日间 | 固定浅色主题 |
| 夜间 | 固定深色主题 |
| 自动 | 跟随系统设置 |

要求：三种模式可在设置页切换，切换即时生效，并持久化到本地。

---

## 五、语言支持

| 语言 | Locale |
|------|--------|
| 中文 | `zh` |
| 英文 | `en` |
| 日语 | `ja` |

要求：界面文案、设置项、Todo 占位符等全部可本地化；语言切换即时生效并持久化。

---

## 六、UI 风格

- Material Design 3
- 统一色彩、字体、组件风格
- 导航形态随屏幕尺寸自适应（手机底部栏、平板/桌面侧栏）

---

## 七、功能规格（按页面）

### 7.1 首页

- 应用标题与简短介绍
- 待办统计（总数、未完成数，来自 Todo 数据）
- 快捷入口：跳转 Todo、设置
- 当前主题与语言状态展示（可选）
- 响应式：平板/桌面可并排卡片，手机纵向堆叠

### 7.2 设置页

- **主题**：日间 / 夜间 / 自动（单选）
- **语言**：中文 / English / 日本語（单选）
- **关于**：版本号、开源说明（可选）
- 设置写入本地，下次启动恢复

### 7.3 TodoMVC 页

参考 [TodoMVC](https://todomvc.com/) 核心能力：

| 功能 | 说明 |
|------|------|
| 新增 | 输入框 + 添加按钮/回车 |
| 列表 | 展示所有 Todo |
| 完成/未完成 | 勾选切换 |
| 编辑 | 双击或编辑按钮修改标题 |
| 删除 | 单条删除 |
| 过滤 | All / Active / Completed |
| 批量 | 「标记全部完成」「清除已完成」 |
| 计数 | 未完成数量 |
| 持久化 | 本地存储，重启不丢 |

可选增强（非必须）：滑动删除、空状态插画。

### 7.4 全局导航

- 三页之间可切换，当前页高亮
- 按屏幕宽度选择导航形态（详见技术方案）

---

## 八、待确认项

1. **应用名称**：仓库名 `astra`，应用显示名是否也用「Astra」？
2. **品牌色**：是否有指定主色（否则使用默认 seed color）？
3. **Todo 存储**：Hive（推荐）还是仅 `shared_preferences`（更简单）？
4. **导航**：移动端 BottomBar + 桌面 Rail，还是全平台统一 Drawer？
